{
  "name": "react-scoped-styles",
  "private": true,
  "workspaces": [
    "packages/*/*",
    "*/next-project/*",
    "*/vite-project/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e"
  },
  "devDependencies": {
    "turbo": "^1.12.4"
  }
} 